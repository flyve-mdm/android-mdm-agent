<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp"
        android:id="@+id/linearLayout">

        <Switch
            android:id="@+id/swNotifications"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_margin="5dp"
            android:text="@string/disable_notifications" />

        <Switch
            android:id="@+id/swConnectionNotification"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_margin="5dp"
            android:text="@string/enable_connection_notifications" />

        <Switch
            android:id="@+id/swtDrawOverlay"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_margin="5dp"
            android:text="@string/option_draw_overlay" />

        <Switch
            android:id="@+id/swDarkTheme"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/enable_dark_theme"
            android:layout_margin="5dp"
            android:visibility="gone"
            />

        <Button
            android:id="@+id/btnClear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/delete_data"
            style="?android:attr/borderlessButtonStyle"
            android:layout_gravity="right"
            android:layout_marginTop="40dp"
            android:textColor="@color/red"
            android:visibility="gone"
            />
    </LinearLayout>
</ScrollView>
